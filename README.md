# homebrew-tap

`CCManager` 的 Homebrew tap。

## 安装

```bash
brew install --cask zwmmm/tap/ccmanager
```

或者先添加 tap：

```bash
brew tap zwmmm/tap
brew install --cask ccmanager
```

## 说明

- GUI 应用通过 `Casks/ccmanager.rb` 分发
- 版本和 `sha256` 由 `zwmmm/CCManager` 的发布流水线自动更新
